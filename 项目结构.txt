资管邮箱密码：27CB0CE449E676dB、196327D699058fCD

ankSync/
├── data/
│   ├── CV2/
│   │   ├── ningbo_duizhangdandayin.bmp
│   │   ├── target_printer.bmp
│   │   ├── save_as_pdf_default.bmp
│   │   ├── save_as_pdf_hover.bmp
│   │   └── save_button.bmp
│   ├── login/
│   │   ├── ningbo_bank.png
│   │   └── other_site.png  # 示例其他网站图标
│   ├── 方正兰亭准黑_GBK.ttf
│   └── S.ico
├── playwright-browsers/
├── config.json
├── gui.py
├── ningbo_bank.py
├── hangzhou_bank.py
├── utils.py
├── login_manager.py
├── todo_manager.py
├── AI.py


添加新银行
1.添加图标
2.修改config 添加账号密码网址
3.login_manager添加登录代码
4.gui import new bank
4.gui 字典添加 new bank（下拉菜单会自动更新）
5.编辑new_bank.py代码
6修改login_manager映射SITE_HANDLERS = {
登录代码注意读取的config是否正确username, password, login_url, config_path = read_bank_config(project_root, "zhongxin_bank")

其他修改方式
page.locator('input[name="startDate"]').evaluate('element => { element.value = "2025-07-25"; element.dispatchEvent(new Event("input", { bubbles: true })); element.dispatchEvent(new Event("change", { bubbles: true })); }')
page.locator('input[name="startDate"]').evaluate('element => { element.value = "2025-06-01"; element.dispatchEvent(new Event("input", { bubbles: true })); element.dispatchEvent(new Event("change", { bubbles: true })); }')


添加新页面
1.添加pages字典  pages = [ {"icon": ft.Icons.TASK_OUTLINED, "selected_icon": ft.Icons.TASK_ROUNDED, "label": "新页面",
         "content": new_page_content}, ] # 新页面
2.编写new_page_content = ft.Column(
3.窗口尺寸字典


添加流水筛选
statement-processor增加字典
修改下方
                elif "平安银行" in filename:
                    bank_type = "pingan_bank"


page.locator('input[name="开始日期").evaluate("(el, value) => { el.removeAttribute('readonly'); el.value = value; el.dispatchEvent(new Event('input', { bubbles: true })); el.dispatchEvent(new Event('change', { bubbles: true })); }", "2025-07-25")

page.locator('input[name="结束日期").evaluate("(el, value) => { el.removeAttribute('readonly'); el.value = value; el.dispatchEvent(new Event('input', { bubbles: true })); el.dispatchEvent(new Event('change', { bubbles: true })); }", "2025-06-01")
page.locator('input[name="开始日期"]').evaluate("(el, value) => { el.removeAttribute('readonly'); el.value = value; el.dispatchEvent(new Event('input', { bubbles: true })); el.dispatchEvent(new Event('change', { bubbles: true })); }", "2025-07-25")
page.get_by_role("textbox", name="开始日期").nth(1).evaluate("(el, value) => { el.removeAttribute('readonly'); el.value = value; el.dispatchEvent(new Event('input', { bubbles: true })); el.dispatchEvent(new Event('change', { bubbles: true })); }", "2025-07-25")
